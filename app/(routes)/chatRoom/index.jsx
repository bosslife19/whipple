import React from 'react';
import { StyleSheet, View } from 'react-native';
import ChatScreen from '../../../screen/chat/ChatRoom';

const Index = () => {
    return (
        <View>
            <ChatScreen/>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Index;
