import React from 'react';
import { StyleSheet, View } from 'react-native';
import NotificationList from '../../../screen/notifications/notifications';

const Notifi = () => {
    return (
        <View>
            <NotificationList/>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Notifi;
